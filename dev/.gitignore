# Everything Composer generates, plus anything a visual test site creates.
# The tracked part of dev/ is just: composer.json, phpunit.xml and tests/.

/vendor/
/kirby/
composer.lock
.phpunit.cache/
/coverage/

# Optional visual test site (see DEVELOPMENT.md) — real images and Panel
# accounts must never be committed to the plugin repo.
/index.php
/content/
/media/
/site/
