# OS junk
.DS_Store
Thumbs.db

# Node
node_modules/
dist/
build/
.vite/
.parcel-cache/
public/build/

# Composer / PHP
/vendor/
/kirby/
/kirby
composer.phar

# Local Kirby dev site for visual testing — holds real images and Panel
# accounts, neither of which belong in the plugin repo. Recreate with:
#   composer dev:setup
/dev/
*.log
# Libraries typically don't commit composer.lock
composer.lock

# Kirby MCP (local dev tool, not part of the plugin)
/.kirby-mcp/
/.cache/

# Environment
.env
.env.*

# Coverage / tests
/coverage/
phpunit.xml.cache
.phpunit.cache/

# IDE / editor
/.idea/
/.vscode/
*~
CLAUDE.md
