{
  "name": "ianhobbs-kirby-swiper-block",
  "version": "1.0.3",
  "description": "Kirby CMS layout block plugin — Swiper 12 slider with panel editor",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --config vite.panel.config.js",
    "build": "vite build --config vite.panel.config.js"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.0",
    "vite": "^8.0.16"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
